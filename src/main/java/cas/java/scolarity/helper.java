package cas.java.scolarity;

import org.springframework.data.repository.Repository;

import javax.lang.model.element.Element;

public class helper {
    /*private static <T extends Element, R extends Repository<T>> void saveElement(T element,R repository){
        repository.save(element);
    }*/
}
